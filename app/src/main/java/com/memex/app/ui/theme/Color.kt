package com.memex.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Backgrounds ──────────────────────────────────────────────────────────────
val MemexBlack      = Color(0xFF070710)   // true-black canvas
val MemexDeepNavy   = Color(0xFF0D0D1A)   // surface base
val MemexCard       = Color(0xFF12122A)   // card background
val MemexCardBorder = Color(0xFF1E1E40)   // card stroke / dividers

// ── Brand purple ─────────────────────────────────────────────────────────────
val MemexPurple     = Color(0xFF7B5CF0)   // primary interactive
val MemexPurpleLight= Color(0xFF9D7FF5)   // hover / lighter tint
val MemexPurpleDim  = Color(0xFF3D2E78)   // container / pressed state

// ── Accent teal ──────────────────────────────────────────────────────────────
val MemexTeal       = Color(0xFF00D4AA)   // secondary / success
val MemexTealDim    = Color(0xFF004D3D)   // teal container

// ── Semantic ─────────────────────────────────────────────────────────────────
val MemexAmber      = Color(0xFFF59E0B)   // warning
val MemexRed        = Color(0xFFEF4444)   // error / destructive

// ── Text / neutral ───────────────────────────────────────────────────────────
val MemexWhite      = Color(0xFFF8F8FF)   // primary text
val MemexGray       = Color(0xFF8888AA)   // secondary / caption text
val MemexGrayDim    = Color(0xFF2A2A45)   // subtle divider / border
